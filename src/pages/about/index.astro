---
import { EXTERNAL_LINKS } from "@/constants/links";
import Layout from "@/layouts/Layout.astro";
import Icon_Github from "@/svg/icon_github.svg";
import Icon_X from "@/svg/icon_x.svg";
---

<Layout title="About">
  <h1 class="h1">About</h1>
  <section>
    <h2 class="h2">このサイトについて</h2>
    <p>
      主にフロントエンドの面で個人的に学んだことや、技術的な知見をアウトプットしていくためのサイトです。
    </p>
  </section>
  <section>
    <h2 class="h2">著者について</h2>
    <h3 class="h3">yskm_dev</h3>
    <ul class="social">
      <li class="socialItem">
        <Icon_X aria-label="X" />:
        <a class="socialLink" href={EXTERNAL_LINKS.x} target="_blank"
          >@_yskm_dev_</a
        >
      </li>
      <li class="socialItem">
        <Icon_Github aria-label="GitHub" />:
        <a class="socialLink" href={EXTERNAL_LINKS.github} target="_blank"
          >yskm-dev</a
        >
      </li>
    </ul>
    <p>
      東京在住。<br />
      10年以上、フロントエンドエンジニアとしてWeb制作の現場に関わってきました。 主にHTML/CSS/JavaScriptを用いたコーディングや、演出面での実装を得意としています。最近はAstroやNext.jsなどのモダンなフレームワークや、Webアクセシビリティを主に勉強中です。
    </p>
  </section>
</Layout>

<script></script>

<style lang="scss">
  @use "@/styles/variables" as var;
  @use "@/styles/extension" as *;

  .main {
    width: 100%;
    max-width: calc(rem(640) + var(--margin-l) * 2);
    padding: 0 var(--margin-l);
    margin: auto;
  }

  .social {
    display: flex;
    gap: var(--margin-m);
    margin-bottom: var(--margin-m);
  }

  .socialItem {
    display: flex;
    gap: var(--margin-xxs);
    align-items: center;

    svg {
      width: rem(16);
      height: rem(16);
      fill: var(--color-text);
    }

    a {
      text-decoration: underline;
      text-underline-offset: rem(4);
    }
  }

  @media (hover: hover) {
    .socialItem {
      a:hover {
        color: var(--color-primary);
      }
    }
  }

  .h1 {
    margin-block: var(--margin-l);
    font-size: var(--font-size-heading-1);
    font-weight: 600;
    line-height: var(--line-height-heading-1);
  }

  .h2 {
    margin-block: var(--margin-l);
    font-size: var(--font-size-heading-2);
    font-weight: 600;
    line-height: var(--line-height-heading-2);
  }

  .h3 {
    margin-top: var(--margin-l);
    margin-bottom: var(--margin-s);
    font-size: var(--font-size-heading-3);
    font-weight: 600;
    line-height: var(--line-height-heading-3);
  }

  .h4 {
    margin-block: var(--margin-m);
    font-size: var(--font-size-heading-4);
    font-weight: 600;
    line-height: var(--line-height-heading-4);
  }

  .p-base {
    margin-block: var(--margin-s);
    font-size: var(--font-size-base);
    line-height: var(--line-height-base);
  }

  .p-large {
    margin-block: var(--margin-s);
    font-size: var(--font-size-large);
    line-height: var(--line-height-base);
  }

  .p-small {
    margin-block: var(--margin-s);
    font-size: var(--font-size-small);
    line-height: var(--line-height-base);
  }

  .border-bottom {
    padding-bottom: var(--margin-xs);
    border-bottom: rem(1) solid var(--color-border);
  }

  .box {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    margin-bottom: var(--margin-l);
    font-weight: 600;
    color: var(--color-background);
    background-color: var(--color-text);

    &[data-size="xxl"] {
      height: var(--margin-xxl);
      font-weight: 600;
    }

    &[data-size="xl"] {
      height: var(--margin-xl);
    }

    &[data-size="l"] {
      height: var(--margin-l);
    }

    &[data-size="m"] {
      height: var(--margin-m);
    }

    &[data-size="s"] {
      height: var(--margin-s);
    }

    &[data-size="xs"] {
      height: var(--margin-xs);
    }

    &[data-size="xxs"] {
      height: var(--margin-xxs);
    }
  }

  .palletList {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(rem(120), 1fr));
    gap: var(--margin-l);
  }
</style>
